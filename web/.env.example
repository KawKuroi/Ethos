# Configuración pública de Supabase para la web (Auth, D26).
# Se rellenan en Vercel (y en .env.local para desarrollo). Son valores públicos
# (la anon key está protegida por RLS), pero no se versionan valores reales.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
