// La tarjeta de X/Twitter es la misma que la OG: se reexporta para que Next
// emita también twitter:image (summary_large_image usa el mismo 1200×630).
export { default, size, contentType, alt } from "./opengraph-image";
